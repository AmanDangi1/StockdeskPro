rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open rules for development (allows any user to read/write any data)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
